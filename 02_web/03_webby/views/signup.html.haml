.centerbox

  %h1 Sign Up

  %hr{:size => 1, :width => 300}

  - form_for @user, @action || "/signup", :method => :POST do |f|
    = error_messages_for @user
    -#What is the proper way to implement error_messages, reference site was down earlier check again
    = csrf_tag
    %table{:align => :center}
      %tr
        %td= f.label :name, :caption => "Name:"
        %td= f.text_field :name
    
      %tr
        %td= f.label :email, :caption => "Email:"
        %td= f.text_field :email

      %tr
        %td= f.label :password, :caption => "Password:"
        %td= f.password_field :password
     
      %tr
        %td 
        %td
          = f.submit
          = link_to 'Cancel', '/'

  %hr{:size => 1, :width => 300}